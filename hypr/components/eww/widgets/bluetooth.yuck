(defwidget bluetooth [?text class]
    (box
        :class "${class}"
        :space-evenly "true"
        :orientation "h"
        :spacing "0"
        (button
            :class "${class}"
            :onclick "$HOME/.config/hypr/scripts/bluetooth/toggleBluetoothMenu.sh"
;            :onclick "( $HOME/.config/hypr/components/rofi/bluetooth/bluetooth.sh & )"
            "󰂯"
        )
    )
)