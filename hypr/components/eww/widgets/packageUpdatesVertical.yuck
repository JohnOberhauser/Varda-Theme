(defpoll packages_update :interval "5m" "$HOME/.config/hypr/scripts/updates/checkUpdates.sh newLine")

(defwidget packageUpdatesVertical [?text class]
    (button
        :class "${class}"
        :onclick "gtk-launch org.manjaro.pamac.manager &> /dev/null &"
        packages_update
    )
)