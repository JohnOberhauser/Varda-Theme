;(include "./widgets/weather.yuck")

(defwidget cal []
    (box
        :class "calendar"
        :space-evenly false
        :orientation "v"
        (calendar)
        (weather)
    )
)

(defwidget calendarContainer []
    (box
        :class "calendarContainer"
        :width 320
        (calendar
            :class "realCalendar"
            :show-heading false
        )
    )
)