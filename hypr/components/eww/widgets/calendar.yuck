;(include "./widgets/weather.yuck")

(defwidget cal []
    (box
        :class "calendar"
        :space-evenly false
        :orientation "v"
        (calendar)
        (weather)
    )
)

(defwidget calendarContainer []
    (box
        :class "calendarContainer"
        :width 400
        (calendar
            :class "realCalendar"
        )
    )
)